'use strict';
console.log('--- loading: add-word.js');

const addWordHandler = () => {
  debugger;
  const userInput = prompt('');
  if (_) {
    return;
  }

  words._(_);
  const newMessage = ``;
  displayHandler(newMessage);
};
